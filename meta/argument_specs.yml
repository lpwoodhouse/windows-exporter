# see https://docs.ansible.com/ansible/latest/user_guide/playbooks_reuse_roles.html#role-argument-validation
---
#argument_specs:
#  # {{ role_name }}/tasks/main.yml entry point
#  main:
#    short_description: The main entry point for the {{ role_name }} role.
#    options:
#      <variable>:
#        type: "int" # int,str,bool,float,list,dict,raw
#        required: false # true,false
#        default: 10050 # use if required is false
#        description: "This should be a port value from 1000-65535"
argument_specs:
  main:
    short_description: The main entry point for the lpwoodhouse.windows_exporter role.
    options:
      _windows_exporter_version:
        type: "str"
        required: true
        description: "This should be a string value equating to the desired release version"
      _windows_exporter_msi_local_dir:
        type: "str"
        required: false
        description: "This should be a string value for the directory containing the "




_windows_exporter_version: latest
_windows_exporter_msi_local_dir: ""
_windows_exporter_download_dir: '%USERPROFILE\Downloads'